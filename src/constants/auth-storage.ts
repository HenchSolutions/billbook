/** Persisted after signup/login so the login form can prefill organization code. */
export const LAST_ORGANIZATION_CODE_KEY = "billbook_last_organization_code";

/** Cached session snapshot from last successful GET /auth/me — must match AuthContext storage. */
export const BILLBOOK_SESSION_KEY = "billbook_session";
